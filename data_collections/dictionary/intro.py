# elements of dictionary are in the form of key value pair
# key and value together formed is element
# key shouldnot be repeated..keys should be unique
#mutable
#keys are homogeneous and values are hetrogeneous
#nesting possible
#values duplication allowed



dict={'name':'arya','age':'22','class':'first'}
print(dict)
print(type(dict))

# all keys should be in the same form

print("dict[name]:",dict['name'])
 # we need to call key eg here name ,age,classes are keys